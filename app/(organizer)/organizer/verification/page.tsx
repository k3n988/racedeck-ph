"use client";
import { FormEvent, useEffect, useState } from 'react';
type Verification={status:string;legal_name:string|null;business_name:string|null;registration_number:string|null;reviewer_notes:string|null;document_storage_references:unknown[]};
export default function VerificationPage(){const [v,setV]=useState<Verification|null>(null);const [form,setForm]=useState({legal_name:'',business_name:'',registration_number:''});const [message,setMessage]=useState('Loading verification…');const [busy,setBusy]=useState(false);async function load(){const r=await fetch('/api/organizer/verification');if(!r.ok){setMessage('Verification could not be loaded.');return}const d=await r.json();setV(d);if(d)setForm({legal_name:d.legal_name??'',business_name:d.business_name??'',registration_number:d.registration_number??''});setMessage('')}useEffect(()=>{void load()},[]);async function save(e:FormEvent){e.preventDefault();setBusy(true);const r=await fetch('/api/organizer/verification',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,legal_name:form.legal_name||null,business_name:form.business_name||null,registration_number:form.registration_number||null})});setBusy(false);setMessage(r.ok?'Draft saved.':(await r.json()).error??'Save failed.');await load()}async function upload(e:FormEvent<HTMLFormElement>){e.preventDefault();const input=e.currentTarget.elements.namedItem('file');if(!(input instanceof HTMLInputElement)||!input.files?.[0])return;setBusy(true);const b=new FormData();b.append('file',input.files[0]);const r=await fetch('/api/organizer/verification/documents',{method:'POST',body:b});setBusy(false);setMessage(r.ok?'Document uploaded.':(await r.json()).error??'Upload failed.');e.currentTarget.reset();await load()}async function submit(){setBusy(true);const r=await fetch('/api/organizer/verification/submit',{method:'POST'});setBusy(false);setMessage(r.ok?'Submitted for review.':(await r.json()).error??'Submission failed.');await load()}const locked=v?.status==='pending_review'||v?.status==='approved';return <main className="mx-auto max-w-3xl space-y-6 p-6"><h1 className="text-2xl font-semibold">Organizer verification</h1><p className="text-sm">Status: {v?.status??'draft'}</p>{message&&<p role="status" className="text-sm">{message}</p>}{v?.reviewer_notes&&<p className="rounded border p-3 text-sm">Reviewer feedback: {v.reviewer_notes}</p>}<form onSubmit={save} className="space-y-4">{(['legal_name','business_name','registration_number'] as const).map(k=><label key={k} className="block text-sm capitalize">{k.replaceAll('_',' ')}<input disabled={locked} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-1 w-full rounded border p-2"/></label>)}<button disabled={busy||locked} className="rounded bg-black px-4 py-2 text-white">Save draft</button></form><form onSubmit={upload}><input disabled={busy||locked} required name="file" type="file" accept="application/pdf,image/jpeg,image/png"/><button disabled={busy||locked} className="ml-2 rounded border px-4 py-2">Upload document</button></form>{!locked&&<button disabled={busy} onClick={submit} className="rounded bg-black px-4 py-2 text-white">Submit for review</button>}</main>}
