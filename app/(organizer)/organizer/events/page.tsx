'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';
type Event = { id: string; name: string; event_date: string; lifecycle_status: string; review_status: string; confirmed_count: number; capacity: number | null; gross_sales: number };
export default function OrganizerEventsPage() { const [events, setEvents] = useState<Event[]>([]); const [search, setSearch] = useState(''); const [message, setMessage] = useState('Loading events…'); const [open, setOpen] = useState(false); const [form, setForm] = useState({ name: '', slug: '', event_date: '', venue: '' }); async function load() { const response = await fetch(`/api/organizer/events?search=${encodeURIComponent(search)}`); const body = await response.json().catch(() => ({})); if (!response.ok) { setMessage(body.error ?? 'Events could not be loaded.'); return; } setEvents(body.events ?? []); setMessage(''); } useEffect(() => { void load(); }, []); async function create(event: React.FormEvent) { event.preventDefault(); const response = await fetch('/api/organizer/events', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }); const body = await response.json().catch(() => ({})); if (!response.ok) { setMessage(body.error ?? 'Event could not be created.'); return; } setOpen(false); setForm({ name: '', slug: '', event_date: '', venue: '' }); await load(); } return <main className="mx-auto max-w-7xl space-y-6 p-6"><header className="flex items-start justify-between"><div><h1 className="text-3xl font-semibold">Events</h1><p className="text-gray-600">Manage events for your organization.</p></div><button onClick={() => setOpen(true)} className="rounded bg-black px-4 py-2 text-sm text-white">Create Event</button></header><form onSubmit={(e) => { e.preventDefault(); void load(); }} className="flex gap-2"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search events" className="rounded border p-2" /><button className="rounded border px-3">Search</button></form>{message && <p role="status" className="text-sm">{message}</p>}{!message && <div className="overflow-x-auto rounded border"><table className="w-full text-left text-sm"><thead className="border-b bg-gray-50"><tr><th className="p-3">Event</th><th className="p-3">Date</th><th className="p-3">Lifecycle</th><th className="p-3">Review</th><th className="p-3">Registrations</th><th className="p-3">Sales</th><th className="p-3">Actions</th></tr></thead><tbody>{events.map((event) => <tr key={event.id} className="border-b"><td className="p-3 font-medium">{event.name}</td><td className="p-3">{event.event_date}</td><td className="p-3">{event.lifecycle_status}</td><td className="p-3">{event.review_status}</td><td className="p-3">{event.confirmed_count} / {event.capacity ?? '∞'}</td><td className="p-3">PHP {event.gross_sales.toFixed(2)}</td><td className="p-3"><Link href={`/organizer/events/${event.id}`} className="underline">Open workspace</Link></td></tr>)}{events.length === 0 && <tr><td colSpan={7} className="p-6">No events found.</td></tr>}</tbody></table></div>}{open && <div className="fixed inset-0 z-10 flex items-center justify-center bg-black/40 p-4"><form onSubmit={create} className="w-full max-w-md space-y-3 rounded bg-white p-6"><h2 className="text-xl font-semibold">Create Event</h2><input required placeholder="Event name" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="w-full rounded border p-2" /><input required placeholder="URL slug" value={form.slug} onChange={(e) => setForm({ ...form, slug: e.target.value })} className="w-full rounded border p-2" /><input required type="date" value={form.event_date} onChange={(e) => setForm({ ...form, event_date: e.target.value })} className="w-full rounded border p-2" /><input placeholder="Venue" value={form.venue} onChange={(e) => setForm({ ...form, venue: e.target.value })} className="w-full rounded border p-2" /><div className="flex justify-end gap-2"><button type="button" onClick={() => setOpen(false)} className="rounded border px-3 py-2">Cancel</button><button className="rounded bg-black px-3 py-2 text-white">Create draft</button></div></form></div>}</main>; }
