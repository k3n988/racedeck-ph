'use client';

import Link from 'next/link';
import { useCallback, useEffect, useState } from 'react';
import { useSearchParams } from 'next/navigation';

type EventRow = { id: string; name: string; banner_url: string | null; event_date: string; venue: string | null; address: string | null; lifecycle_status: string; review_status: string; registration_availability: string; registrations_count: number; confirmed_count: number; capacity: number | null; gross_sales: number };
const columns = ['Event', 'Date & Location', 'Lifecycle Status', 'Review Status', 'Registration Availability', 'Registrations', 'Capacity', 'Gross Sales', 'Actions'];
const money = (value: number) => new Intl.NumberFormat('en-PH', { style: 'currency', currency: 'PHP', maximumFractionDigits: 0 }).format(Number(value) || 0);
const label = (value: string) => value.replaceAll('_', ' ');
function Badge({ value }: { value: string }) { const positive = ['published', 'ongoing', 'open'].includes(value); const neutral = ['completed', 'approved', 'not_submitted'].includes(value); return <span className={`inline-flex whitespace-nowrap rounded-full px-2.5 py-1 text-[11px] font-bold capitalize ${positive ? 'bg-emerald-100 text-emerald-800' : neutral ? 'bg-blue-100 text-blue-800' : value === 'rejected' || value === 'cancelled' ? 'bg-red-100 text-red-800' : 'bg-amber-100 text-amber-800'}`}>{label(value)}</span>; }
function EmptyRow() { return <tr><td colSpan={9} className="p-14 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-orange-50 text-2xl text-orange-600" aria-hidden="true">🏁</div><h2 className="mt-4 font-bold text-[#071b41]">No events yet</h2><p className="mt-1 text-sm text-slate-500">Create your first race event to start building your event workspace.</p></td></tr>; }

export default function OrganizerEventsPage() {
  const searchParams = useSearchParams();
  const [events, setEvents] = useState<EventRow[]>([]); const [message, setMessage] = useState('Loading events...'); const [busy, setBusy] = useState(false);
  const search = searchParams.get('search') ?? ''; const lifecycleStatus = searchParams.get('lifecycle_status') ?? '';
  const load = useCallback(async () => { setMessage('Loading events...'); const controller = new AbortController(); const timeout = window.setTimeout(() => controller.abort(), 15000); try { const query = new URLSearchParams(); if (search) query.set('search', search); if (lifecycleStatus) query.set('lifecycle_status', lifecycleStatus); const response = await fetch(`/api/organizer/events${query.toString() ? `?${query.toString()}` : ''}`, { cache: 'no-store', signal: controller.signal }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error ?? 'Events could not be loaded.'); setEvents(body.events ?? []); setMessage(''); } catch (error) { setMessage(error instanceof DOMException && error.name === 'AbortError' ? 'Events request timed out. Check the dev server and Supabase connection, then retry.' : error instanceof Error ? error.message : 'Events could not be loaded.'); } finally { window.clearTimeout(timeout); } }, [lifecycleStatus, search]);
  useEffect(() => { void load(); }, [load]);
  async function deleteEvent(eventId: string, eventName: string) { if (!window.confirm(`Delete "${eventName}"? This cannot be undone.`)) return; setBusy(true); setMessage('Deleting event...'); try { const response = await fetch(`/api/organizer/events/${eventId}`, { method: 'DELETE' }); const body = await response.json().catch(() => ({})); if (!response.ok) { setMessage(body.error ?? 'Event could not be deleted.'); return; } setEvents((current) => current.filter((event) => event.id !== eventId)); setMessage('Event deleted.'); } catch { setMessage('Event could not be deleted. Check your connection and try again.'); } finally { setBusy(false); } }
  const loading = message.startsWith('Loading');
  return <main className="w-full max-w-none space-y-6 bg-[#f5f7fa] p-5 sm:p-7">{!loading && message && <div className="flex items-center justify-between rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800"><span>{message}</span><button onClick={() => void load()} className="rounded-lg border border-red-300 bg-white px-3 py-1.5 font-bold">Retry</button></div>}<div className="w-full overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="w-full min-w-[1280px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50"><tr>{columns.map((column) => <th key={column} className="p-3 text-xs font-bold uppercase tracking-wide text-slate-500">{column}</th>)}</tr></thead>{loading ? <tbody><tr>{columns.map((column) => <td key={column} className="p-4"><div className="h-4 animate-pulse rounded bg-slate-200" /></td>)}</tr><tr>{columns.map((column) => <td key={column} className="p-4"><div className="h-4 animate-pulse rounded bg-slate-200" /></td>)}</tr></tbody> : events.length === 0 ? <tbody><EmptyRow /></tbody> : <tbody className="divide-y divide-slate-100">{events.map((event) => { const fill = event.capacity ? Math.min(100, Math.round((event.confirmed_count / event.capacity) * 100)) : 0; return <tr key={event.id}><td className="p-3 font-bold text-[#071b41]">{event.name}</td><td className="p-3"><p>{new Date(event.event_date).toLocaleDateString('en-PH', { month: 'short', day: 'numeric', year: 'numeric' })}</p><p className="max-w-40 truncate text-xs text-slate-500">{event.venue ?? event.address ?? 'Location to be announced'}</p></td><td className="p-3"><Badge value={event.lifecycle_status} /></td><td className="p-3"><Badge value={event.review_status} /></td><td className="p-3"><Badge value={event.registration_availability} /></td><td className="p-3"><b>{event.confirmed_count.toLocaleString()}</b><p className="text-xs text-slate-500">{event.registrations_count.toLocaleString()} total</p></td><td className="p-3">{event.capacity ? <><p>{event.confirmed_count.toLocaleString()} / {event.capacity.toLocaleString()}</p><div className="mt-1 h-1.5 w-20 rounded-full bg-slate-200"><div className="h-1.5 rounded-full bg-orange-500" style={{ width: `${fill}%` }} /></div></> : 'Unlimited'}</td><td className="p-3 font-bold text-[#071b41]">{money(event.gross_sales)}</td><td className="p-3"><div className="flex gap-2"><Link href={`/events/${event.id}`} className="rounded-lg border px-3 py-1.5 text-xs font-bold">Preview</Link><Link href={`/organizer/events/${event.id}`} className="rounded-lg bg-[#071b41] px-3 py-1.5 text-xs font-bold text-white">Manage</Link><button type="button" disabled={busy} onClick={() => void deleteEvent(event.id, event.name)} className="rounded-lg border border-red-200 px-3 py-1.5 text-xs font-bold text-red-700 disabled:cursor-not-allowed disabled:opacity-50">Delete</button></div></td></tr>; })}</tbody>}</table>{!loading && events.length > 0 && <div className="border-t border-slate-100 px-4 py-3 text-xs text-slate-500">Showing 1–{events.length} of {events.length} events</div>}</div></main>;
}
