import { NextResponse } from 'next/server';
import { requireOrganizerPermission } from '@/lib/auth/organizer-access';
export async function GET() { const auth = await requireOrganizerPermission('view_financials'); if ('response' in auth) return auth.response; const { data, error } = await auth.admin.from('payouts').select('id,payout_reference,status,currency,gross_registration_sales,refunds_deducted,platform_fees_deducted,processing_fees_deducted,net_payout_amount,initiated_at,paid_at').eq('organization_id', auth.membership.organization_id).order('initiated_at', { ascending: false }).limit(500); if (error) return NextResponse.json({ error: 'Payouts could not be loaded' }, { status: 500 }); const paid = (data ?? []).filter((row) => row.status === 'paid').reduce((sum, row) => sum + Number(row.net_payout_amount), 0); return NextResponse.json({ payouts: data ?? [], total_paid_out: paid }); }
