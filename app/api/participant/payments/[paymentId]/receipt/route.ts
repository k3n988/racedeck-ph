import { NextResponse } from 'next/server';
import { createAdminClient } from '@/lib/supabase/admin';
import { createClient } from '@/lib/supabase/server';
import { getPaymentDocument, paymentDocumentPdf } from '@/lib/documents/payment-document';

export async function GET(_request: Request, { params }: { params: { paymentId: string } }) { const supabase = await createClient(); const { data: auth } = await supabase.auth.getUser(); if (!auth.user) return NextResponse.json({ error: 'Authentication required' }, { status: 401 }); const document = await getPaymentDocument(supabase, params.paymentId, auth.user.id, 'receipt'); if (!document) return NextResponse.json({ error: 'Receipt not found' }, { status: 404 }); const receipt = document.document as { id: string; receipt_reference: string; storage_reference: string | null }; const admin = createAdminClient(); if (receipt.storage_reference) { const signed = await admin.storage.from('payment-documents').createSignedUrl(receipt.storage_reference, 600); if (!signed.error && signed.data?.signedUrl) return NextResponse.redirect(signed.data.signedUrl); } const path = `payments/${params.paymentId}/receipt.pdf`; const bytes = await paymentDocumentPdf(document); const uploaded = await admin.storage.from('payment-documents').upload(path, bytes, { contentType: 'application/pdf', upsert: false }); if (!uploaded.error || uploaded.error.message.toLowerCase().includes('already exists')) await admin.from('payment_receipts').update({ storage_reference: path }).eq('id', receipt.id); const signed = await admin.storage.from('payment-documents').createSignedUrl(path, 600); if (!signed.error && signed.data?.signedUrl) return NextResponse.redirect(signed.data.signedUrl); return new NextResponse(bytes as BodyInit, { headers: { 'Content-Type': 'application/pdf', 'Content-Disposition': `attachment; filename="${receipt.receipt_reference}.pdf"`, 'Cache-Control': 'private, no-store' } }); }
