import { NextResponse } from 'next/server';
import { createClient } from '@/lib/supabase/server';
export async function GET() { const supabase = await createClient(); const { data: auth } = await supabase.auth.getUser(); if (!auth.user) return NextResponse.json({ error: 'Authentication required' }, { status: 401 }); const { data, error } = await supabase.from('registrations').select('id,event_id,registration_number,created_at,events(id,name,event_date),race_categories(name),results!inner(id,bib_code,bib_number,participant_display_name,category_name,gun_time,overall_rank,classification_rank,category_rank,result_status,result_batches!inner(publication_status))').eq('user_id', auth.user.id).eq('results.result_batches.publication_status', 'published').order('created_at', { ascending: false }); if (error) return NextResponse.json({ error: 'Results could not be loaded' }, { status: 500 }); return NextResponse.json({ results: data ?? [] }); }
